{
  "name": "class-config",
  "version": "2.3.0",
  "description": "Package that creates and instance of the supplied Class, decorated with class-transform and class-validator decorators",
  "keywords": [
    "config",
    "class-config",
    "env"
  ],
  "author": "Stefan Hariton",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:haritonstefan/class-config.git"
  },
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "rm -Rf ./dist && tsc",
    "test": "jest",
    "test:coverage": "jest --collect-coverage"
  },
  "files": [
    "dist/***",
    "README.md"
  ],
  "dependencies": {
    "class-transformer": "^0.3.2",
    "class-validator": "^0.13.1"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.20",
    "jest": "^26.6.3",
    "reflect-metadata": "^0.1.13",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
